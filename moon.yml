$schema: https://moonrepo.dev/schemas/project.json

tasks:
  build:
    command: tsdown
    toolchain: node
    inputs:
      - src/**/*
      - tsdown.config.ts
    outputs:
      - dist/**/*
  test:
    command: vitest
    toolchain: node
    inputs:
      - src/**/*
      - vitest.config.ts
